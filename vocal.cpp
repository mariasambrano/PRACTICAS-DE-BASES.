#include <iostream>
using namespace std;
int main(){
	char letra;
	cout<<"digita una letra ";
	cin>>letra;
	if(letra == 'a' or letra == 'e' or letra == 'i' or letra == 'o'  or letra == 'u')
    cout<<"es vocal";
    else
    cout<<"no es vocal";
    
    return 0;
		}
